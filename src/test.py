def makeCalculation(inputNum):
    return inputNum + 100

result = makeCalculation(1000)
print(result)

first_cycle_result = first_cycle_func()
second_cycle_result = second_cycle_func(first_cycle_result)
